import React from 'react';

const page = () => {
    throw new Error('Manual Error')
    return (
        <main>
            <div className="">
                about
            </div>
        </main>
    );
};

export default page;